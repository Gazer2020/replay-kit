# tools

公共工具说明目录。当前可用工具由 `src/replay_kit/` 提供：

- `runner`：`screen` 启动、状态查询、summary 生成。
- `metadata`：实验身份、环境、状态记录。
- `notifier`：飞书通知和 dry-run payload。
- `summary`：从 config、metadata、metrics、日志尾部生成 summary。
