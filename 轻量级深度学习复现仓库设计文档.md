# 轻量级深度学习复现仓库设计文档

## 0. 项目定位

这个仓库不是一个通用深度学习框架，也不是完整 MLOps 系统，而是一个**面向 PyTorch 单卡复现实验的轻量级复现工作仓库**。

它的核心目标是：

1. 让不同方法的复现代码可以各自独立存在；
2. 避免日志、运行、通知、总结这些重复性工作反复重写；
3. 让 Agent 可以按照明确文档参与复现、调试和总结；
4. 让 1–3 人的小规模协作不会污染主线；
5. 让实验结果至少能追溯到：配置、命令、日志、metadata、summary。

项目不追求统一所有模型代码，也不追求复杂实验管理系统。
统一的是**运行入口、记录方式、总结要求、通知流程、协作规则**。

---

# 1. 设计原则

## 1.1 轻量优先

只保留必要机制：

```text
Hydra 配置
screen 运行
基础日志
metadata 记录
summary 总结
飞书 webhook 通知
简单 git branch 协作
Agent 指导文档
```

不引入：

```text
Slurm
Docker
MLflow
DVC
WandB
复杂状态机
复杂任务系统
复杂 checkpoint registry
多机多卡分布式
```

---

## 1.2 方法代码允许杂乱，但产物必须最低限度统一

不同复现方法可以有不同训练代码、数据处理代码、评估代码。

但每次实验至少应该产生：

```text
metadata
log
summary
```

推荐产物包括：

```text
outputs/
  runs/
    method_name/
      experiment_name/
        run_id/
          metadata.json
          config.yaml
          train.log
          summary.md
          checkpoints/
```

其中：

* `metadata.json`：记录实验身份、环境、命令、状态等；
* `config.yaml`：Hydra 最终解析后的配置快照；
* `train.log`：运行日志；
* `summary.md`：实验总结；
* `checkpoints/`：可选，作为本次实验的子目录保存，不单独设置仓库级 checkpoint 目录。

---

## 1.3 Agent 是主要执行者之一

这个仓库要面向 Agent 友好设计。

也就是说，仓库里不只要有代码，还要有清晰的说明文档，让 Agent 能够知道：

```text
应该在哪里放方法代码
应该如何新增一个复现方法
应该如何写配置
应该如何运行实验
应该如何写 summary
应该如何判断实验是否完成
应该如何向飞书发送结果
应该如何维护 branch
```

因此，文档系统是这个仓库的核心部分，而不是附属部分。

---

# 2. 推荐仓库结构

建议整体结构如下：

```text
repro-lab/
  README.md
  AGENTS.md
  requirements.txt
  .env.example
  .gitignore

  configs/
    base.yaml
    methods/
      method_a/
        default.yaml
        debug.yaml
        exp001.yaml
      method_b/
        default.yaml

  methods/
    method_a/
      README.md
      AGENTS.md
      train.py
      eval.py
      summarize.md
      reports/
        report_template.md
    method_b/
      README.md
      AGENTS.md

  tools/
    README.md
    runner/
      README.md
    notifier/
      README.md
    summarizer/
      README.md

  tasks/
    README.md
    task_template.md
    active/
      method_a_repro.md
    done/

  data/
    README.md
    method_a/
      README.md
    method_b/
      README.md

  outputs/
    README.md
    runs/

  docs/
    project_rules.md
    experiment_rules.md
    git_rules.md
    feishu_rules.md
    agent_workflow.md
    summary_rules.md
```

这里的关键点是：

* `methods/` 放每个复现方法的代码；
* `configs/` 放 Hydra 配置；
* `tools/` 放公共工具说明和后续实现；
* `tasks/` 放小规模协作任务；
* `data/` 只保留目录和 README，实际数据通过软链接指向机器本地路径；
* checkpoint 作为实验产物保存在对应 run 目录下；
* `outputs/` 存运行结果和可选 checkpoint，默认不进 Git；
* `docs/` 放给人和 Agent 看的规则文档。

---

# 3. 配置系统设计

## 3.1 使用 Hydra 作为实验配置核心

所有实验参数都应该尽量进入 Hydra 配置，包括：

```text
方法参数
数据路径
输出路径
GPU 选择
seed
训练轮数
batch size
学习率
checkpoint 策略
日志路径
运行命令相关参数
是否发送飞书通知
```

这样可以避免实验命令过长，也方便之后复盘。

---

## 3.2 配置分层

建议配置分为三层。

第一层是全局基础配置：

```text
configs/base.yaml
```

负责记录所有方法都可能用到的字段，例如：

```text
project_name
method_name
experiment_name
seed
device
gpu_id
output_root
data_root
checkpoint_policy
log_policy
notify_policy
```

第二层是方法默认配置：

```text
configs/methods/method_a/default.yaml
```

负责记录某个方法的默认参数。

第三层是具体实验配置：

```text
configs/methods/method_a/exp001.yaml
configs/methods/method_a/debug.yaml
```

用于某次正式实验或调试实验。

---

## 3.3 配置快照

每次实验运行时，都应该在本次实验目录里保存最终配置快照。

推荐保存为：

```text
config.yaml
```

这样之后查看实验结果时，不需要重新追溯当时命令行覆盖了哪些参数。

---

# 4. 实验输出设计

## 4.1 最小输出要求

每次实验至少需要产生：

```text
metadata.json
train.log
summary.md
```

如果实验失败，则至少需要产生：

```text
metadata.json
train.log
failure_summary.md 或 summary.md
```

失败实验也要能通知飞书。

---

## 4.2 metadata 设计

`metadata.json` 是实验追溯的核心。

建议包含：

```text
run_id
project_name
method_name
experiment_name
branch
commit
command
working_directory
hostname
gpu_id
start_time
end_time
status
config_path
resolved_config_path
log_path
summary_path
checkpoint_path
error_message
```

其中 `status` 只需要简单几种：

```text
running
finished
failed
```

不需要设计复杂状态机。

---

## 4.3 日志设计

暂时不做结构化日志。

每个实验只需要保留原始日志：

```text
train.log
```

要求：

1. 训练过程 stdout 和 stderr 尽量进入同一个日志；
2. 报错信息必须可在日志中找到；
3. 日志路径写入 metadata；
4. 飞书失败通知中应该带上日志路径和关键错误摘要。

---

## 4.4 summary 设计

每个实验完成后必须生成 `summary.md`。

summary 不要求所有方法统一指标，但要求统一基本结构。

推荐结构：

```text
# 实验总结

## 基本信息
- 方法：
- 实验名：
- branch：
- commit：
- 配置：
- 日志：

## 复现目标
说明这次实验想复现什么结果。

## 实验设置
说明数据集、模型、关键参数、训练设置。

## 结果
列出本方法关心的核心指标。

## 结论
说明是否达到预期。

## 问题与备注
记录异常、偏差、未解决问题、下一步建议。
```

不同方法可以在此基础上扩展自己的指标。

---

# 5. 方法目录设计

每个复现方法单独放在：

```text
methods/method_name/
```

推荐结构：

```text
methods/method_name/
  README.md
  AGENTS.md
  train.py
  eval.py
  summarize.md
  reports/
    report_template.md
```

## 5.1 README.md

给人看的方法说明。

内容包括：

```text
方法来源
复现目标
依赖说明
数据准备
运行方式
已有实验
当前问题
```

## 5.2 AGENTS.md

给 Agent 看的执行指南。

内容包括：

```text
这个方法要复现什么
代码入口在哪里
配置文件在哪里
数据路径如何设置
应该运行哪些实验
如何判断是否成功
summary 应该包含哪些指标
失败时优先检查什么
```

## 5.3 summarize.md

每个方法自己的总结规则。

例如：

```text
本方法 summary 必须包含：
- 数据集名称
- 训练 epoch
- 最终指标
- 最佳指标
- 是否达到论文目标
- 与目标差距
- 失败原因或可疑点
```

这里可以先用文档约束，不必一开始实现复杂 summarizer。

---

# 6. 运行系统设计

## 6.1 只支持远程服务器单卡运行

运行系统只考虑：

```text
单机
单卡
screen
手动指定 GPU
```

不考虑：

```text
多卡
DDP
Slurm
Kubernetes
Docker
任务队列
```

---

## 6.2 screen 运行器职责

后续可以实现一个简单的 screen runner。

它的职责是：

```text
读取 Hydra 配置
创建实验输出目录
记录 metadata
启动 screen session
把日志写入 train.log
实验结束后触发 summary
实验失败或完成后触发飞书通知
```

不需要提供复杂状态查询。

人只需要能做到：

```text
启动实验
查看 screen
查看日志
收到通知
```

---

## 6.3 实验运行流程

推荐流程如下：

```text
1. 选择 method 和 config
2. 启动 screen 实验
3. 自动创建 run 目录
4. 保存 config 快照
5. 写入 metadata
6. 训练日志进入 train.log
7. 实验结束
8. 生成 summary.md
9. 飞书通知完成或失败
```

---

# 7. checkpoint 设计

checkpoint 不作为强制要求。

默认策略：

```text
不保存 checkpoint
或每个实验最多保存一个 checkpoint
```

checkpoint 不再使用仓库根目录的独立文件夹，而是放在对应实验目录中：

```text
outputs/runs/{method_name}/{experiment_name}/{run_id}/checkpoints/
```

如果方法确实需要保存 checkpoint，建议只保留：

```text
latest checkpoint
或 best checkpoint
```

不建议默认保存大量中间 checkpoint。

每个方法可以在自己的 README 中说明：

```text
是否保存 checkpoint
保存在哪里
是否需要手动清理
```

大 checkpoint 不进入 Git。

---

# 8. 飞书通知设计

## 8.1 通知场景

只需要支持两种：

```text
实验完成
实验失败
```

不需要支持：

```text
中间指标刷新
磁盘告警
GPU 告警
周期性状态报告
```

---

## 8.2 通知内容

实验完成时发送：

```text
实验名
方法名
branch
commit
运行机器
GPU
开始时间
结束时间
耗时
summary 摘要
summary 路径
日志路径
```

实验失败时发送：

```text
实验名
方法名
branch
commit
运行机器
GPU
失败时间
错误摘要
日志路径
```

---

## 8.3 飞书 webhook 管理

飞书 webhook 不写入仓库。

通过环境变量提供，例如：

```text
FEISHU_WEBHOOK
```

仓库中只保留：

```text
.env.example
docs/feishu_rules.md
```

`.env` 必须加入 `.gitignore`。

---

# 9. 数据与大文件设计

## 9.1 Git 中只保留目录结构

数据、预训练模型、checkpoint、实验输出都不进入 Git。

仓库中只保留：

```text
data/
  README.md
  method_a/
    README.md

outputs/
  README.md
```

实际数据通过软链接或本地路径映射。checkpoint 随实验 run 目录保存在 `outputs/runs/` 下。

---

## 9.2 每个方法的数据说明

每个方法的数据 README 至少说明：

```text
需要哪些数据
推荐目录结构
本机软链接应该指向哪里
是否需要预处理
预处理后文件长什么样
```

例如设计目标是：

```text
data/method_a/ 只是仓库里的统一入口
实际可能链接到 /mnt/datasets/xxx
```

这样不同机器可以有不同真实路径，但仓库内部路径保持一致。

---

# 10. 环境设计

只使用：

```text
requirements.txt
```

不强制使用：

```text
uv
conda
poetry
docker
```

但每个方法的 README 应该说明：

```text
是否有额外依赖
是否需要特殊版本
是否需要额外安装包
是否需要编译扩展
```

建议在实验 metadata 中记录基本环境信息：

```text
Python 版本
PyTorch 版本
CUDA 可用情况
GPU 名称
hostname
```

这样出问题时方便排查。

---

# 11. 协作与 branch 设计

## 11.1 小规模 branch 策略

由于只有 1–3 人，branch 不超过 5 个，不需要复杂协作系统。

推荐分支：

```text
main
repro/method-a
repro/method-b
debug/method-a
```

其中：

* `main` 保持相对干净；
* 每个复现方法可以有自己的 `repro/xxx` 分支；
* 临时调试可以用 `debug/xxx`；
* 完成后再合并到 `main`。

---

## 11.2 合并标准

合并到 `main` 前，至少满足：

```text
方法目录存在
方法 README 存在
方法 AGENTS.md 存在
配置文件存在
至少有一次实验 summary
数据说明清楚
不会把大文件提交进 Git
```

不要求一定达到论文结果，但必须说明：

```text
当前复现到了什么程度
和目标差距在哪里
下一步问题是什么
```

---

# 12. 任务分配设计

任务系统只用 Markdown，不用 YAML。

任务文件放在：

```text
tasks/active/
```

完成后移动到：

```text
tasks/done/
```

任务模板只需要包含：

```text
# 任务名称

## 复现内容
说明要复现哪篇论文、哪个方法、哪个实验。

## 目标
说明希望达到什么结果。

## Branch
说明使用哪个 branch。

## 当前状态
未开始 / 进行中 / 已完成 / 暂停。

## 备注
记录数据、环境、问题、结论。
```

这已经足够支撑 1–3 人的小规模协作。

---

# 13. Agent 工作流设计

这是本项目很重要的一部分。

建议在根目录放：

```text
AGENTS.md
```

其中 `AGENTS.md` 是 Agent 的项目级入口和完整项目协作指南。同时每个方法目录也放：

```text
methods/method_name/AGENTS.md
```

## 13.1 根目录 AGENTS.md

根目录 Agent 文档说明整个仓库规则：

```text
你应该先阅读什么
如何理解仓库结构
如何新增一个方法
如何新增配置
如何运行实验
如何写 summary
哪些文件不能修改
哪些文件不能提交
如何处理失败实验
如何发送飞书通知
```

## 13.2 方法级 AGENTS.md

方法级 Agent 文档说明具体复现任务：

```text
论文或方法目标
当前代码状态
训练入口
评估入口
配置入口
数据要求
关键指标
summary 要求
常见失败原因
下一步优先事项
```

## 13.3 Agent 执行流程

推荐 Agent 每次接手任务时按这个流程：

```text
1. 阅读根目录 AGENTS.md
2. 阅读任务 md
3. 阅读对应方法的 README.md 和 AGENTS.md
4. 检查配置文件
5. 检查数据 README
6. 运行 debug 配置
7. 修复明显问题
8. 运行正式配置
9. 生成 summary.md
10. 更新任务状态
11. 必要时准备飞书通知内容
```

---

# 14. 报告模板设计

每个方法可以有简单报告，不需要复杂复现报告系统。

推荐位置：

```text
methods/method_name/reports/
```

报告模板：

```text
# 方法复现报告

## 方法
说明复现的方法或论文。

## 复现目标
说明目标实验、目标指标或目标现象。

## 实验设置
说明数据集、模型、训练参数、环境。

## 结果
给出当前实验结果。

## 与目标差距
说明和论文结果、官方结果或预期结果的差距。

## 问题
记录遇到的问题。

## 结论
说明是否完成复现，以及下一步建议。
```

---

# 15. 文档系统设计

建议 `docs/` 中至少包含这些文档：

```text
docs/project_rules.md
docs/experiment_rules.md
docs/git_rules.md
docs/feishu_rules.md
docs/agent_workflow.md
docs/summary_rules.md
```

## 15.1 project_rules.md

说明项目总原则：

```text
这是轻量复现仓库
不是通用训练框架
不是复杂 MLOps 系统
不保存大文件
不强制统一模型代码
只统一实验记录和总结
```

## 15.2 experiment_rules.md

说明实验规范：

```text
如何命名实验
如何写配置
输出目录要求
metadata 要求
日志要求
summary 要求
checkpoint 策略
```

## 15.3 git_rules.md

说明协作规范：

```text
branch 命名
main 合并标准
哪些文件不能提交
如何处理 outputs
如何处理 data 和 checkpoint
```

## 15.4 feishu_rules.md

说明飞书通知规范：

```text
webhook 从哪里读取
什么时候通知
通知包含哪些内容
失败时如何通知
```

## 15.5 agent_workflow.md

说明 Agent 工作流程：

```text
新增方法流程
调试实验流程
正式实验流程
失败排查流程
summary 生成流程
任务更新流程
```

## 15.6 summary_rules.md

说明 summary 的统一最低要求：

```text
必须包含基本信息
必须包含复现目标
必须包含实验设置
必须包含结果
必须包含结论
失败实验必须说明失败原因
```

---

# 16. 推荐的实验目录命名

推荐每次实验输出到：

```text
outputs/runs/{method_name}/{experiment_name}/{run_id}/
```

其中 `run_id` 可以包含：

```text
日期
时间
短 commit
可选随机后缀
```

例如：

```text
outputs/runs/method_a/exp001/20260513_153000_ab12cd/
```

这样方便根据方法和实验名查找结果。

---

# 17. `.gitignore` 设计原则

应忽略：

```text
.env
outputs/runs/
data/*/*
*.pth
*.pt
*.ckpt
*.safetensors
__pycache__/
*.log
```

但要保留：

```text
data/README.md
data/method_x/README.md
outputs/README.md
```

也就是说，大文件不提交，结构说明要提交。

---

# 18. 最小可用版本设计

第一阶段只需要完成这些：

```text
1. 仓库目录结构
2. README.md
3. AGENTS.md
4. requirements.txt
5. .env.example
6. .gitignore
7. docs/ 下的规则文档
8. configs/base.yaml 的设计
9. 一个示例 method 目录
10. 一个示例 task md
11. 一个示例 summary 模板
12. 一个飞书通知规则文档
```

此时可以还没有完整 runner 代码，但仓库结构和规则已经确定。

---

# 19. 第二阶段再实现的功能

等目录和文档稳定后，再实现公共工具：

```text
screen runner
metadata writer
log collector
summary trigger
feishu notifier
```

实现顺序建议：

```text
1. metadata 记录
2. screen 启动实验
3. 日志写入
4. 实验完成/失败判断
5. 飞书通知
6. summary 生成辅助
```

不要一开始就做太多自动化。先保证能稳定服务真实复现实验。

---

# 20. 这个项目最终应该长什么样

最终这个仓库应该提供一种简单工作模式：

```text
我要复现一个新方法
→ 新建 method 目录
→ 写 README 和 AGENTS 指南
→ 写 Hydra 配置
→ 准备数据软链接
→ 用 screen runner 跑实验
→ 自动记录 metadata 和日志
→ 实验结束后生成 summary
→ 飞书收到完成或失败通知
→ 根据结果更新 task 和 report
```

它的价值不在于把所有方法统一成一个训练框架，而在于：

```text
复现代码可以不统一
但实验流程统一
记录方式统一
总结要求统一
通知方式统一
Agent 工作方式统一
协作边界统一
```

这样既不会过度工程化，又能显著减少你在不同复现项目之间反复搭日志、通知、总结、目录结构的时间。
