# Copy to .env on a remote server if Feishu notifications should be sent.
# Leave unset for local tests; replay-kit will automatically write a dry-run
# notification payload into each run directory.
FEISHU_WEBHOOK=
REPLAY_KIT_NOTIFY_DRY_RUN=
